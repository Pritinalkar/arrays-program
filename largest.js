const numbers =[30,70,1500,240,230,240]

let largetsNumber = numbers[0]

 for(number of numbers ){
     if(largetsNumber<number){
         largetsNumber = number
     }
 }

 console.log(`largest number is: ${largetsNumber}`)